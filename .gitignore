# Secrets — API keys and secret URL paths live only here, on the VPS, or in n8n credentials
.secrets/
.env
.env.local
.env.*.local

# Dependencies and build output
node_modules/
__pycache__/
*.pyc

# Logs and temporary files
*.log
*.tmp
*.bak

# Scratch folder contents (keep folder, exclude contents)
scratch/*
!scratch/.gitkeep

# OS and editor files
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# Claude Code local settings (machine-specific; .claude/settings.json IS committed)
.claude/settings.local.json
