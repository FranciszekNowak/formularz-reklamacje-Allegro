# Architecture — current state

Last updated: 2026-09-23

Read this first. `STATUS.md` is the session-by-session log and open-decision list; this file
is the standing description of what exists and how it fits together. Decisions with full
reasoning live in `architecture/decisions/`.

---

## 1. What the system is for

To be written once the project goal in `CLAUDE.md` is settled.

## 2. What exists

Nothing yet.

## 3. What it borrows from other repos

Runs on the shared VPS owned by `Haxe_agent_system`. Every identifier it depends on
belongs in `contracts/agent-system.md`.
